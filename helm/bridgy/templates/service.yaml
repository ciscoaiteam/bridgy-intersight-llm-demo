apiVersion: v1
kind: Service
metadata:
  name: {{ include "bridgy.fullname" . }}
  labels:
    {{- include "bridgy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bridgy.selectorLabels" . | nindent 4 }}
